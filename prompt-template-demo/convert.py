from pptx import Presentation; import sys; prs = Presentation(sys.argv[1]); content = [f"# {prs.core_properties.title or \"Presentation\"}\n"]; [content.extend([f"\n## Slide {i+1}\n", *[f"{shape.text}\n" for shape in slide.shapes if shapehas_text_frame and shape.text.strip()], "\n---\n"]) for i, slide in enumerate(prs.slides)]; open(sys.argv[2], "w").write("".join(content)); print(f"✅ Converted {sys.argv[1]} to {sys.argv[2]}")
